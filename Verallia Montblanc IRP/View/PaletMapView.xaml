<UserControl x:Class="IRP.View.PaletMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IRP.View"
             xmlns:vm="clr-namespace:IRP.ViewModel"             
             xmlns:mock="clr-namespace:IRP.ViewModel.DesignData"
             xmlns:cc="clr-namespace:IRP.View.Controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance mock:MockPaletMapVm, IsDesignTimeCreatable=True}"
             d:DesignHeight="800" d:DesignWidth="800"
             >
    <UserControl.DataContext>
        <vm:PaletMapViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <ItemsControl ItemsSource="{Binding Palets}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid IsItemsHost="True"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <cc:PaletView Height="50"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>


    </StackPanel>
</UserControl>
